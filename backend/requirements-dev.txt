-r requirements.txt

# Testes
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.5.0
coverage==7.3.2
factory-boy==3.3.0

# Lint/format
black==23.12.1
flake8==6.1.0
isort==5.13.2
pre-commit==3.6.0

# Segurança/estática (compatível com packaging novo)
bandit==1.7.5
# Removi o safety aqui para evitar conflito. Quando quiser usar, instale a versão
# mais nova (3.x) separadamente:  pip install "safety>=3"
# safety>=3

# Logging extra / validação extra
colorlog==6.8.0
structlog==23.2.0
cerberus==1.3.5
jsonschema==4.20.0

# Dev utils
watchdog==3.0.0
memory-profiler==0.61.0
# Perf (evita instalar no Windows)
line-profiler==4.1.1; platform_system != "Windows"
